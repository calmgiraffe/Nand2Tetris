class Main {                           
    function void main() {
        var int x, y, z; // local 0, 1, 2
        var boolean bool; // local 3
        
        let x = 1 + 2;
        let y = 5 + ((3*x - 1) + (x - 4));
        let z = x / y;
        }
}
